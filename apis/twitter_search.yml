info:
  name: twitter_search
  type: http
  seq: 9

http:
  method: post
  url: "{{host}}/api/twitter/search"
  headers:
    - name: Content-Type
      value: application/json
    - name: X-API-Key
      value: "{{api_key}}"
    - name: X-Grabby-Token
      value: "{{api_key}}"
  body:
    type: json
    data: |-
      {
        "query": "golang",
        "limit": 40
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
