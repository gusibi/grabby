info:
  name: kick
  type: http
  seq: 6

http:
  method: post
  url: http://localhost:5040/api/browsers/kick
  headers:
    - name: Content-Type
      value: application/json
    - name: X-API-Key
      value: your_api_key
  body:
    type: json
    data: |-
      {
        "conn_id": "ws_browser:browser-tools"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
