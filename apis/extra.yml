info:
  name: extra
  type: http
  seq: 2

http:
  method: post
  url: "{{host}}/api/extract"
  headers:
    - name: Content-Type
      value: application/json
    - name: X-API-Key
      value: "{{api_key}}"
    - name: X-Grabby-Token
      value: "{{api_key}}"
  body:
    type: json
    data: |-
      {
        "url": "https://x.com/mikenevermiss/status/2066401066518802637"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
